version: "3.8"
services:
  web:
    build: .
    image: hpi-web:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
    restart: unless-stopped 